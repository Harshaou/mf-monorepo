/**
 * Async boundary for standalone development (the remote also runs on its own port so
 * the team can develop in isolation). Mandatory because react/react-dom are shared.
 */
import('./bootstrap');

export {};
